import React from "react";
// import { Request } from "@components";

import "./root.scss";

export const Root = (props) => {
  console.log(props, "Root");
  return (
    <div id="wolf-root">
      <button> </button>
    </div>
  );
};
